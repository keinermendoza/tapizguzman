---
const {body, title} = Astro.props;
---
<div class="flex flex-col h-full rounded-lg  dark:bg-gray-800 dark:border-gray-700">
    <slot />
    <h5 class="mb-2 text-xl font-semibold tracking-tight text-gray-900 dark:text-white">{title}</h5>
    <p class="text-sm font-normal text-gray-500 dark:text-gray-400">{body}</p>
</div>
